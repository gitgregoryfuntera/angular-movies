import { VoteValuePipe } from './vote-value.pipe';

describe('VoteValuePipe', () => {
  it('create an instance', () => {
    const pipe = new VoteValuePipe();
    expect(pipe).toBeTruthy();
  });
});
